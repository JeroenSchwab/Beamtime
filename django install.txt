# in vertual env do:

#Install Django
python -m pip install Django

#install django-bootstrap x
pip install django-bootstrap4

#install bootstrap-datepicker-plus
pip install django-bootstrap-datepicker-plus

#install django-crispy-forms
pip install django-crispy-forms

start the shell in linux
pipenv shell

start the shell in windows
activate in dir Scripts

start the server
python manage.py runserver

make migrations after changing model.py
python manage.py makemigrations

make the migration
python manage.py migrate
